.container
  .page-header
    h1 User Login
  .span8.offset2
    .hero-unit
      p Login to your Kolabria account
      form.form-horizontal.users#formLogin(name='ulogin', action='/ulogin', method='post')
        .control-group
          label.control-label(for="email") Email:
          .controls
            input.input-xlarge#email(required, type="email", name='user[Email]', value=user.Email || '', placeholder="Email@email.com")
        .control-group
          label.control-label(for="password") Password:
          .controls
            input.input-xlarge#password(required, name='user[password]', value=user.password || '', type='password', placeholder="Password")
        .control-group
          input.btn.btn-primary.pull-right(type='submit', value='Log In')
        .control-group
          a.pull-left(href='/pwreset') Forgot your password?
          a.pull-right(href='/register') Create a new account

  script

