extends sitelayout
block localjs
    script
      $('#formLogin').submit(function(e){
        e.preventDefault();
        _gaq.push(['_trackEvent','Form','Submit','Login']);
        var form = this;
        console.log('logging submit')
        setTimeout(function(){ form.submit()}, 150);
       });